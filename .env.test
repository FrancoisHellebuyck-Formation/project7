# Configuration pour les tests RAGAS
# Ce fichier contient les paramètres spécifiques aux tests d'évaluation

# Délai entre les appels API Mistral (en secondes)
# Permet d'éviter de dépasser les limites de quota (rate limiting)
#
# Ce délai est appliqué:
# 1. Après chaque appel dans les tests individuels (test_rag_retrieval_quality, etc.)
# 2. Avant l'évaluation RAGAS (pour laisser le quota se récupérer)
#
# Recommandations selon votre tier Mistral AI:
# - Free tier: 5.0 secondes (quota très limité)
# - Paid tier: 2.0 secondes (valeur par défaut)
# - Augmentez si vous rencontrez des erreurs 429
RAGAS_MISTRAL_DELAY=8.0

# Configuration de timeout pour les requêtes API (en secondes)
# Ce timeout est appliqué:
# 1. Aux appels API dans get_rag_response() (tests individuels)
# 2. Au LLM ChatMistralAI utilisé par RAGAS (évaluation des métriques)
#
# Recommandations:
# - 30 secondes: valeur par défaut (suffisant pour la plupart des requêtes)
# - 60 secondes: si vous avez des questions longues ou complexes
# - Augmentez si vous rencontrez des timeouts
RAGAS_API_TIMEOUT=30

# Nombre de contextes à récupérer pour chaque question
RAGAS_TOP_K=8

# Nombre de tentatives maximales en cas d'erreur 429 (rate limiting)
# En cas d'échec, le script retente automatiquement la requête
RAGAS_MISTRAL_RETRY=3

# Note: Le modèle Mistral utilisé est configuré dans .env via MISTRAL_MODEL
# Par défaut: mistral-small-latest
# L'API charge cette configuration au démarrage
#
# Important: L'évaluation RAGAS effectue plusieurs appels API pour chaque métrique
# (faithfulness, answer_relevancy, context_precision, context_recall)
# Le nombre total d'appels = nb_questions × nb_métriques × nb_étapes_par_métrique
# Exemple: 3 questions × 4 métriques = 12+ appels API minimum
